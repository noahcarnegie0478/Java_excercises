/**
 * TASK: Balanced String
 * Source image: exercise/m-20-BalancedString.png
 *
 * A string is called balanced when every letter occurring in the string appears
 * both in upper- and lowercase. For example, the string "CATattac" is balanced
 * ('A', 'C' and 'T' occur in both cases), but "Madam" is not ('D' and 'A' appear
 * only in lowercase). Note that the number of occurrences does not matter.
 *
 * Write a function:
 *     class Solution { public int solution(String S); }
 * that, given a string S of length N, returns the length of the shortest balanced
 * fragment of S. If S does not contain any balanced fragment, the function should
 * return -1.
 *
 * Examples:
 * 1. Given S = "azABaabza", the function should return 5.
 *    The shortest balanced fragment of S is "ABaab".
 * 2. Given S = "TacoCat", the function should return -1. There is no balanced fragment.
 * 3. Given S = "AcZCbaBz", the function should return 8. The shortest balanced
 *    fragment is the whole string.
 * 4. Given S = "abcdefghijklmnopqrstuvwxyz", the function should return -1.
 *
 * Assume that:
 * - N is an integer within the range [1..200];
 * - string S consists only of letters ('a'-'z' and/or 'A'-'Z').
 *
 * In your solution, focus on correctness. The performance of your solution will
 * not be the focus of the assessment.
 */
public class Solution {

    // ==================== YOUR CODE ====================
    // Implement this method. Nothing else needs to change.

    public int solution(String S) {
        System.out.println("testing git 4");
        throw new UnsupportedOperationException("TODO: implement");
    }
}
